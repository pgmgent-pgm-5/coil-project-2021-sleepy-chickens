import React from "react";
import styled from "styled-components";

const Container = styled.div`
  width: 50%;
`;

interface Props {}

const DeliveryAddressForm = (props: Props) => {
  return <Container>Delivery address</Container>;
};

export default DeliveryAddressForm;
